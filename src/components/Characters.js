export default function Characters(props){
    console.log(props);
    const {characters} = props

    return (
        <div>
            <h1> Desde characters </h1>
          


            
           
        </div>
       
    );
}